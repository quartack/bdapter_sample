<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.quartack.bdapter.sample">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".basic.BasicActivity" />
        <activity android:name=".diff_util.DiffUtilActivity" />
        <activity android:name=".drag.DragActivity" />
        <activity android:name=".event.EventActivity" />
        <activity android:name=".filter.FilterActivity" />
        <activity android:name=".grid.GridActivity" />
        <activity android:name=".recycler_inside_recycler.RIRActivity" />
        <activity android:name=".sort.SortActivity" />
        <activity android:name=".tree.TreeActivity" />
        <activity android:name=".update.UpdateActivity" />
        <activity android:name=".variable_name.VariableNameActivity" />

    </application>

</manifest>
